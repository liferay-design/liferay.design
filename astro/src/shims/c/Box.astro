---
import { sxToStyle } from '@lib/sx'
const { sx, style, class: className, as } = Astro.props
const Tag = as || 'div'
const s = [sxToStyle(sx), style].filter(Boolean).join(';')
---
<Tag class={className} style={s || undefined}><slot /></Tag>
